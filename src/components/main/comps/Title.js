export const Title = ({ text }) => {
  return (
    <h2 className="fw-bold fs-1 text-center mb-4 p-3 text-white bg-primary" style={{ borderBottom: '10px solid #e24159' }}>
      {text}
    </h2>
  );
};
